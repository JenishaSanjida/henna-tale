import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    badge: {
        paddingHorizontal: 5,
        paddingVertical: 0,
        borderRadius: 20,
        marginRight: -30
    },
    badgeText: {
        fontSize: 12,
        fontWeight: 'bold',
        textTransform: 'uppercase',
    }
});